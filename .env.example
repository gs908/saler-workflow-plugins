# Server Configuration
PORT=5699

# Application
NODE_ENV=development

# CORS (if needed)
# ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# Route Prefix
# ROUTE_PREFIX=saler-workflow-plugins
